use crate::config::ConfigError;

// This module previously contained config persistence functions that are no longer used
// in the modern Clap-based CLI implementation. The file is kept to maintain module structure.
