title: Validate config set candidates before persistence and during dry-run
status: Todo
relationships:
  parent: DEV-53
priority: High
type: Bug
reporter: mallox
created: 2026-09-06T11:10:31.713363+00:00
description: |-
  DEV-53 second-pass audit follow-up.
  Audit findings: C9.

  config set accepts inconsistent names, dry-run returns before validation, conflict checking contains placeholder PROJ-1 logic, and real updates save before resolved validation. Normalize supported names, build/validate one candidate against real affected tasks, then atomically save; dry-run omits only the write. Acceptance: documented dotted names work, invalid dry-runs fail, rejected changes preserve bytes, and force/conflict semantics are real and tested. DEV-53 normalization safety is a prerequisite already implemented.

  Related umbrella: DEV-53.
  Definition of done: regression coverage, relevant docs/contracts, and repository lint/test/smoke gates; preserve user-owned data.
tags:
- audit-pass-2
